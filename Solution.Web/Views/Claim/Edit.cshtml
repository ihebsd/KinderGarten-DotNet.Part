
@model Solution.Web.Models.ClaimModel

@{
    ViewBag.Title = "Edit";
}
<body>
    <section id="authorization_page" class="authorization_page">
        <div class="container">
            <div class="row">
                <div class="col-md-1"></div>
                <div class="col-md-10 dot">
                    <h2 class="section-title">Update claim</h2>
                </div>
                <div class="col-md-1"></div>
            </div>
            <div class="row">
                <div class="col-md-3">
                    <div class="sidebar">
                        <div class="categories b_green">
                            <h4 class="title">My profile</h4>
                            <ul>
                                <li><a href="@Url.Action("indexfront","Claim")">My claims</a></li>
                                <li><a href="@Url.Action("indexfront","Reputation")"> My reputations</a></li>
                            </ul>
                        </div>

                    </div>
                </div>
                <div class="col-md-5">
                    @using (Html.BeginForm("Edit", "Claim", htmlAttributes: new { id = "createform" }, method: FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="reg_box">
                            @Html.TextBoxFor(model => model.Name, new { @class = "form-control", @placeholder = "Claim name" })
                            @Html.TextAreaFor(model => model.Description, new { @class = "form-control", rows = "5", cols = "3", @placeholder = "Description" })

                            @Html.TextBoxFor(m => m.ClaimType, new { @class = "form-control", @placeholder = "claim type" })



                        </div>

                        <button class="button" type="submit" value="Create">Update</button>
                    }
                    <script>
                        var form = document.getElementById('updateform');

                        function myFunction() {
                            if (form.checkValidity()) {
                                alert("Your claim was successfully updated");
                            }
                        }
                    </script>
                </div>
            </div>
        </div>
    </section>
</body>
<br />
<br />
<br />
<br />
<br />
<br />
<br />

<br />
<br />
<br />
